import {
  defaultAction,
} from '../actions';
import {
  EXAMPLE_ACTION_TYPE,
} from '../constants';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of EXAMPLE_ACTION_TYPE', () => {
      const expected = {
        type: EXAMPLE_ACTION_TYPE,
      };
      expect(defaultAction()).toEqual(expected);
    });
  });
});
